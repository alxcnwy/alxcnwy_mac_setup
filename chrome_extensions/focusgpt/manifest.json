{
  "manifest_version": 3,
  "name": "FocusGPT Snapshot",
  "version": "1.3",
  "description": "One-key focus snapshot: save all tabs in the current Chrome window into Bookmarks/SNAPSHOTS/<timestamp> [tabcount] with a desktop notification.",
  "permissions": ["bookmarks", "tabs", "notifications"],
  "background": { "service_worker": "background.js" },
  "commands": {
    "save_tabs": {
      "suggested_key": {
        "default": "Alt+Shift+0",
        "mac": "Command+Shift+0"
      },
      "description": "Snapshot all tabs in the current window to Bookmarks/SNAPSHOTS/<timestamp>"
    }
  }
}
